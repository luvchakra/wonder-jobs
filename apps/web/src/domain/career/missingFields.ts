/**
 * Employer application forms commonly ask for these; Career DNA has no field for any of them
 * yet. Say so honestly rather than guessing or inventing a value to look complete. Shared by the
 * Application Pack and the browser extension's profile route so the two can never disagree about
 * what's missing.
 */
export const MISSING_CANDIDATE_FIELDS = ["Phone number", "LinkedIn profile", "Location / address"];

# --- Persistence (Supabase project "wonder-ark", schema "wonderjobs"). Leave unset for local-only mode.
SUPABASE_URL=https://jazdtomcgqjxjueedmck.supabase.co
# Service role / secret key from Supabase → Project Settings → API keys. Server-side only. NEVER expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=
# --- Encryption key for BYOK provider secrets (32+ random chars). Required in production.
#     e.g. `openssl rand -base64 48`
WONDER_SECRET_KEY=
